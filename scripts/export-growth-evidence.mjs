#!/usr/bin/env node
/**
 * Exports the Grand Prize evidence package (docs/GROWTH_EVIDENCE.md):
 * verified users, published campaigns, unique viewers, verified interests,
 * accepted intro rooms, K-factor, revenue, refunds, ratings.
 *
 * Not implemented yet — requires the analytics events pipeline (P0 item 10).
 */
console.error('export-growth-evidence: not implemented yet (needs analytics pipeline)');
process.exit(1);
