#!/usr/bin/env node
/**
 * Seeds a demo environment for Devpost review and screenshots.
 * Local database seeding is handled by supabase/seed.sql (see scripts/test-db.sh);
 * this script will additionally provision demo media assets and a reviewer
 * promo unlock once those systems exist.
 */
console.error('seed-demo: not implemented yet (local DB seed lives in supabase/seed.sql)');
process.exit(1);
